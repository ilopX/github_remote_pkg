name: use_remote_pkg
description: A sample command-line application.

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  github_remote_pkg:
    git:
      url: https://github.com/ilopX/github_remote_pkg
      ref: ???
      path: remote_pkg
